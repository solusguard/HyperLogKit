# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HyperLogKit iOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HyperLogKit iOS

  target 'HyperLogKit iOS Tests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'HyperLogKit OSX' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HyperLogKit OSX

  target 'HyperLogKit OSX Tests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'HyperLogKit tvOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HyperLogKit tvOS

  target 'HyperLogKit tvOS Tests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'HyperLogKit watchOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HyperLogKit watchOS

end
